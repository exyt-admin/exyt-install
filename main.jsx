import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './exyt_training_recommender.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
